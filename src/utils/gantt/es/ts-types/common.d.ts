export declare enum InteractionState {
    'default' = "default",
    'grabing' = "grabing",
    'scrolling' = "scrolling"
}
export interface IPosition {
    left: number;
    top: number;
    width: number;
    height: number;
}
